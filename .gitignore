# Compiled binaries (root-anchored so these don't also match the Android
# source dir clients/.../com/alro/sola/).
/sola
/victron-collector

# Local databases
*.db
*.db-shm
*.db-wal

# Local configuration
config.yaml

# VS Code local state
.vscode/

# Claude Code project instructions (local, not shared)
CLAUDE.md